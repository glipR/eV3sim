# Godot-specific ignores
.godot/
export.cfg
export_presets.cfg

# Imported translations (automatically generated from CSV files)
*.translation

# Mono-specific ignores
.mono/
data_*/

# EV3Sim specific ignores
*.pyc
# Think this will be temporary and eventually live in `user://` but for now ignore.
device-data
